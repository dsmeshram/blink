import { NextResponse } from "next/server"


export const POST = async (req: Request, res : Response) => {
    // Get email from user 
    // Send email along with validtoken with validity JWT 10min validation
    return NextResponse.json({status: 400})
}
